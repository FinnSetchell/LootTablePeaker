# Gradle launch options only — these cannot be read from stonecutter.properties.toml.
# Mod metadata and per-version dependency versions live there instead.
org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true
org.gradle.configuration-cache=true
